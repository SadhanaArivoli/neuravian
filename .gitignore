# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
*.db

# Node
node_modules/
frontend/dist/
desktop/build/
desktop/dist/

# Prepared x86 verification fixture (CC0 source data; generated locally)
verification/fixtures/prepared/
verification/x86/evidence/
verification/x86/work/
verification/x86/*.zip

# Local data volume
data/

# Test DICOM data (large binary files — download separately via git clone)
# Source: https://github.com/datalad/example-dicom-structural (PDDL license)
backend/tests/data/dicom-structural/dicoms/

# Env files
.env
.env.local

# AWS deployment live state and private keys
.neuravian-aws/
# Pre-rename live state remains in place for compatibility and must never be committed.
.neuroforge-aws/
*.pem

# Terraform working data, state, private inputs, and generated plans
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
*.tfvars.json
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.known_hosts

# Locally packaged deliverables
.codex-artifacts/

# Live/redacted decommission reports are generated locally, not committed
docs/cloud/decommission-runs/*-decommission-report.md

# macOS
.DS_Store

# Editors
.idea/
.vscode/

# Local Claude Code permissions (machine-specific)
.claude/settings.local.json

# Root-level compiled desktop main (build artifact; Electron entry is desktop/build/main/index.js)
index.js
